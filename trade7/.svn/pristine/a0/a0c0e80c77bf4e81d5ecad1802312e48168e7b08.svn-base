package com.liantuo.trade.bus.factory.process;


import com.liantuo.trade.bus.process.Process;
import com.liantuo.trade.client.trade.packet.head.TradePacketHead;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.testng.AbstractTestNGSpringContextTests;
import org.testng.Assert;
import org.testng.annotations.Test;

import javax.annotation.Resource;


@ContextConfiguration(
        locations = {
                "classpath:config/spring/controller-context.xml",
                "classpath:config/spring/applicationContext-*.xml"
        })
public class TradeProcessFactoryTest extends AbstractTestNGSpringContextTests {
//    @Resource
//    private TradeProcessFactory tradeProcessFactory;
//
//    @Test
//    public void testBuildProcess() throws Exception {
//        Assert.assertNotNull(tradeProcessFactory);
//        TradePacketHead head = new TradePacketHead();
//        head.setVersion("1");
//        head.setRequest_code("1");
//        Process process = tradeProcessFactory.buildProcess(head);
//        Assert.assertNotNull(process);
//    }
}